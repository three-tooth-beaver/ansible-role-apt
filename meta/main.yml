---
galaxy_info:
  author: thulium_drake
  description: Configures APT settings such as repo's, automatic updates and proxy. Requires Ansible Vars Merge
  company:
  role_name: apt

  license: GPLv3

  min_ansible_version: 2.7

  platforms:
    - name: Debian
      versions:
        - stretch
        - buster
    - name: Ubuntu
      versions:
        - bionic

  galaxy_tags:
    - apt
    - updates
    - proxy
    - repository
    - repo
    - autoupdate

dependencies: []
